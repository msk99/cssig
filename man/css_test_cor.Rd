% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-diagnostics.R
\name{css_test_cor}
\alias{css_test_cor}
\title{Correlation among constituent tests and CSS}
\usage{
css_test_cor(
  x,
  method = c("spearman", "pearson", "kendall"),
  title = NULL,
  subtitle = NULL
)
}
\arguments{
\item{x}{A `css_result` still carrying its constituent test columns.}

\item{method}{Correlation method passed to [stats::cor()]. Default
`"spearman"`, which is the appropriate choice here because CSS is itself a
rank statistic.}

\item{title, subtitle}{Plot labels.}
}
\value{
A [ggplot2::ggplot] object. The underlying correlation matrix is
  attached as the `"cor"` attribute.
}
\description{
Randhawa et al. (2014) report low to moderate correlation between pairs of
constituent tests but high correlation between CSS and each of them, which is
the evidence that CSS captures information from across the tests rather than
tracking any single one.
}
\examples{
data(css_sim_small)
res <- css(css_input(css_sim_small,
                     tests = c(fst = "high", xpehh = "high", ddaf = "high")))
css_test_cor(res)

}
